# Compiled binaries (anything without a file extension in the root, or explicit build outputs)
toy
causet_test
*.o
*.out
*.exe

# Data files generated by running the code (keep the folder, ignore contents)
data/*.txt
